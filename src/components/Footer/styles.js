import { colors } from '../../constants/styles';

const styles = {
  footer: {
    position: 'absolute',
    bottom: 0,
    width: '100%',
    height: 60,
    color: colors.white,
    backgroundColor: colors.primary
  }
}

export default styles;
